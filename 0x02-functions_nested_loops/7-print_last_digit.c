#include "main.h"

/**
 * print_last_digit - Returns the value of the last digit.
 *
 * @n: the int to print
 * Return: Always 0.
 */
int print_last_digit(int n)
{
	int digit;

	digit = n % 10
	_putchar (last_digit + '0');
	return (digit);
}
